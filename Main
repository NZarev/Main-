import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        double radians = scanner.nextDouble();
        double degrees = radians * (180.0 / Math.PI);
        System.out.printf("%.10f%n", degrees);
        scanner.close();
    }
}
